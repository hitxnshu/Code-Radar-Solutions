#include<stdio.h>
struct Student{
    int roll;
    char name[100];
    float marks;
}
int main(){
    int n;
    scanf("%d",&n);
    struct Student s1;
    for(int i = 0;i < n;i++){
    scanf("%d",&si.roll);
    scanf("%s",&si.name);
    scanf("%f",&si.marks);
    }
    for(int i = 0;i < n;i++){
        for(int j = i+1;j < n;j++){
            if(si)
        }
    }

}