#include<stdio.h>
int main(){
    char string[100];
    fgets(string,sizeof(string),stdin);
    int word = 1;
    for(int i = 0;i < n;i++){
        whilo(string[i] != '\0'){
            if(a[i] == ''){
               word++;
            }
        }
    }
}
printf("%d",word)