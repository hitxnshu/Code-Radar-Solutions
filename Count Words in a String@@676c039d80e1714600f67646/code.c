#include<stdio.h>
int main(){
    char string[100];
    fgets(string,sizeof(string),stdin);
    int word = 1;
    int i = 0;
        whilo(string[i] != '\0'){
            if(a[i] == ''){
               word++;
            }
            i++;
        }
    }

printf("%d",word)